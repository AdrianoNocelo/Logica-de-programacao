// ========= Como a AI fez: ===========
let temperatura = 25.5;  
let estaChovendo = false;  
let mensagem = `A temperatura é ${temperatura}°C e está chovendo? ${estaChovendo ? "Sim" : "Não"}.`;

console.log(mensagem);

// ========= Como EU fiz: ===========
let temperaturaAtual = 25.5;
let tachovendo = false;

console.log("A temperatura é " + temperaturaAtual + "°C e está chovendo? " + tachovendo + ".");
const numeros = [2, 5, 6, 7, 89, 43, 24]

// length -> retorna a quantidade de itens dentro de uma array

// Inicializacao, condicao e incremento
for (let i = 0; i < numeros.length; i++) {
  console.log(numeros[i])
}


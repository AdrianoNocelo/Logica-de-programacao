//---------------- Printar cada nome da lista ----------------
const names = ["adriano", "gui", "eros", "igor"]

// inicializacao, condicao, incremento
// for (i = 0; i < 4; i++)
for (let i = 0; i < names.length; i++) {
  console.log("---=== " + names[i] + " ===---")
}


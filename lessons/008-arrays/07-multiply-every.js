// Multiplicar todos os numeros de uma lista por 5

const numeros = [3, 5, 6, 7, 8, 10]

// Inicializacao, condicao e incremento
for (let i = 0; i < numeros.length; i++) {
  const multiplicador = numeros[i] * 5;
  console.log(multiplicador)
}
